{
  "devDependencies": {
    "@sendgrid/client": "^6.4.0",
    "@sendgrid/mail": "^6.4.0",
    "dotenv": "^8.1.0",
    "netlify-lambda": "^1.6.3"
  },
  "scripts": {
    "start:lambda": "netlify-lambda serve functions-src"
  }
}
